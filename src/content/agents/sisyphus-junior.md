---
name: Sisyphus-Junior
role: task executor
description: Category-spawned executor. Model is selected automatically based on the task category (visual-engineering → Gemini, quick → GPT Mini, deep → GPT-5.5). Cannot re-delegate — prevents infinite loops. The workhorse of every execution phase.
model: Category-dependent
category: worker
source: omo
domains: []
tools: []
readOnly: false
---

Category-spawned executor. Model is selected automatically based on the task category (visual-engineering → Gemini, quick → GPT Mini, deep → GPT-5.5). Cannot re-delegate — prevents infinite loops. The workhorse of every execution phase.
